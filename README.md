# drp_server

## Installation

macOS and Linux:

```console
curl -fsSL https://deno.land/x/install/install.sh | sh
```

Windows:

```
scoop install deno
```

## Usage

Run server

```
deno run --allow-net --allow-env app.ts
```

Lint

```
deno lint
```

Format

```
deno fmt
```
